4K 跨类别续轨 A/B：最终显示层统计（仅日志）

输入：runs/input_videos/4k/VID_20260712_131410.mp4；首个连续 30 秒窗口。
保持跨类别续轨 A/B 和全部现有参数不变；未修改 IoU、EMA、TTL 或显示规则。

口径：
- detector_empty：与编码帧 PTS 对齐的正式推理结果 `detections=0`。
- overlay_empty：编码帧最终 `overlay_box_count=0`，即画面没有绘制任何框。

1. overlay_box_count=0 视频帧数量：104（编码帧总数 933）
2. 最长连续无显示框时间：1313 ms（46 个视频帧）
3. detector_empty 但仍通过跨类别续轨显示框的帧数：7
4. Track ID 连续保持时间：平均 784.277 ms；最长 12202 ms（101 段显示连续区间）
5. 同一位置出现双框次数：0
6. Shadow Track 仍被绘制的帧数：0

说明：当前 `is_displayable()` 只绘制 Active Track；Shadow Track 不进入最终 overlay，
因此第 6 项为 0。跨类别续轨仅维持原 Track ID、原 classId 与原显示标签。

